# Ignore everything by default
*

# Keep the .gitignore itself tracked
!.gitignore

# Allow activation capture README (case-insensitive safety)
!ACTIVATION_CAPTURE_README.md
!activation_capture_readme.md

# Allow the activation capture code directory and its Python files
!activation_capture/
!activation_capture/**

# Allow the capture orchestration scripts (explicit)
!capture_activations.py

# Allow the activations output directory (raw captured activations)
!activations/
!activations/**

# End - everything else will remain ignored to avoid including proprietary data
